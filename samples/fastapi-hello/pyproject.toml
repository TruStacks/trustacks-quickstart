[project]
name = "fastapi-hello"
version = "0.1.0"
description = "Minimal FastAPI service for the TruStacks quickstart workshop."
requires-python = ">=3.12"
dependencies = ["fastapi>=0.118", "uvicorn[standard]>=0.32"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
